//- do JSON auto marshalling also on messages from client to web service

- avoid getters and setters on the entity classes? java 7?
- setup Spring for injecting the DAOs in the Resources
//- setup GitHub (or SVN?)
//- setup Maven

- tidy up NamingStrategy for db tables
- clean com.example packages (and change Dyno classpath) and index.html
- how to properly manage local and production versions of hibernate.properties 

- log4j or commmons.logging? (both are being used currently: see jog4j.properties and HibernateSessionRequestFilter.java)

- when done with DB
	- in hibernate.properties, comment the lines under "#development config"
