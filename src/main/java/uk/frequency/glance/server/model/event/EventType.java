package uk.frequency.glance.server.model.event;

public enum EventType {
	STAY, HOME, WORK, PUB, EXERCISE, SLEEP, WAKE, WALK, CYCLING, COMUTE, TRAVEL, MEETING, MUSIC, MOVIE, JOIN // TODO this list is not definite
	//TODO persist as string to avoid problems when adding types
} 